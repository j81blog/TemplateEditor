# TemplateEditor
Browser-based editor for Windows optimization templates. Load, edit, validate, and export XML templates that drive registry, service, scheduled task, app removal, PowerShell, and file/folder actions across multiple Windows OS versions. No install, no backend and runs entirely in your browser.
